export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}
export enum OrderStatus {
  PENDING = 'pending',
  PAID = 'paid',
  SHIPPED = 'shipped',
  CANCELLED = 'cancelled',
}
export enum ProductCategory {
  PHONE = 'phone',
  EARPODS = 'earpods',
  WATCH = 'watch',
  LAPTOP = 'macbook',
}